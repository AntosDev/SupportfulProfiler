import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'supportful-profiler-app',
  webDir: 'www'
};

export default config;
